name: Sync Wiki From Docs

on:
  push:
    branches:
      - main
    paths:
      - "docs/**"
      - "scripts/generate_wiki_home.py"
      - ".github/workflows/sync-wiki.yml"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: sync-wiki
  cancel-in-progress: false

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate wiki home page
        run: python scripts/generate_wiki_home.py --output build/wiki/Home.md

      - name: Push generated page to wiki
        env:
          WIKI_TOKEN: ${{ secrets.WIKI_PUSH_TOKEN || github.token }}
          WIKI_REPO: ${{ github.repository }}.wiki.git
        shell: bash
        run: |
          set -euo pipefail

          git clone "https://x-access-token:${WIKI_TOKEN}@github.com/${WIKI_REPO}" wiki
          cp build/wiki/Home.md wiki/Home.md

          cd wiki
          if git diff --quiet -- Home.md; then
            echo "Wiki already matches generated docs hub."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Home.md
          git commit -m "Sync wiki from docs"
          git push
